<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quyền mạng -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanji_Learning_Sakura"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Activity phải ở TRONG application -->
        <activity android:name=".ui.ExamPlayerActivity"/>
        <activity android:name=".ui.ExamSetupActivity"/>
        <activity android:name=".ui.QuizPlayerActivity"/>
        <activity android:name=".ui.QuizSetupActivity"/>
        <activity android:name=".ui.KanjiBrowserActivity"/>
        <activity android:name=".ui.AdminKanjiActivity"/>
        <activity android:name=".ui.CreateLevelActivity"/>
        <activity android:name=".ui.CreateLessonActivity"/>
        <activity android:name=".ui.ProfileActivity"/>
        <activity android:name=".ui.MomoUpgradeActivity"/>
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".ui.LoginActivity"/>
        <activity android:name=".ui.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
